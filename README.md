The xbox Api is using nodejs for the backand at the moment will be using JSON but moving to SQL 
it can be a bit bloated on the file on routers as it will have over 900 games in there 


Once the games are done will be interacting with a different project with PHP, CSS, HTML and ajax(js) when making an dynamic filtering system 
